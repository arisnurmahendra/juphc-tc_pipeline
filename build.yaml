- name: build
  workspaces:
    - name: source
      workspace: pipeline-workspace
  taskRef:
    name: buildah
    kind: ClusterTask
  params:
  - name: IMAGE
    value: "us.icr.io/$(context.pipelineRun.namespace)/$(params.app-name)"
  runAfter:
    - tests
